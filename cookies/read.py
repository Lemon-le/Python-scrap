"""
目标: 实现一个Cookies池的搭建，Cookie池中保存了许多新浪微博账号和登录后的Cookies信息，并且Cookies池还需定时监测
每个Cookies的有效性,如果Cookies无效，那就删除该Cookies并模拟登录生成新的Cookies;同时Cookies池还需要一个重要的接口,
即随机获取Cookies的接口,Cookies运行后，我们只需要请求该接口，即可随机获得一个Cookies并用其爬取

Cookies池架构的基本模块：存储模块、生成模块、检查模块、接口模块
存储模块: 存储模块负责存储每个账号的用户名密码以及每个账号对应的Cookies信息，同时还需要提供一些方法来实现方便的存取操作
生成模块: 生成模块负责生成新的Cookies;此模块会从存储模块逐个拿取账号的用户名和密码，然后模拟登陆目标页面，判断登陆成功,
就将Cookies返回并交给存储模块存储
监测模块: 监测模块需要定时监测数据库中的Cookies,在这里我们需要设置一个检测链接，不同站点的检测链接不一样，检测模块会逐个
拿取账号对应的Cookies去请求链接，如果返回的状态是有效的，那么此Cookies没有失效，否则Cookies失效并移除，接下来等待生成模块
重新生成即可
接口模块：接口模块需要用API来提供对外服务的接口.由于可以的Cookies可能有多个，我们可以随机返回Cookies的接口，这样保证每个
Cookies都有可能被取到，Cookies越多，每个Cookies被取到的机会就缺小，从而减少被封号的风险

"""